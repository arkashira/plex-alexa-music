# Plex Alexa Music
A Python project for testing and quality assurance of VoicePlay.

## Requirements
- Python 3.8+
- pytest

## Installation
1. Clone the repository
2. Install dependencies with `pip install -r requirements.txt`
3. Run tests with `python -m pytest`
